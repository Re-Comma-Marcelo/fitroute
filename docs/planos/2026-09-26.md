# Plano — 2026-09-26

Contexto lido: CLAUDE.md ✓ · PRD ✗ (não existe; visão do produto tirada de CLAUDE.md, README.md e `docs/*.md`) · MELHORIAS.md ✗ (não existe — item 1) · planos anteriores: 0 em `docs/planos/` (usei `.lovable/plan/*` e o git log até 2026-09-25 para não repetir tema) · Supabase: conectado, só leitura — 5 perfis, 3 pessoas treinando (27 treinos, 393 séries, último em 2026-09-25), 3 usando a dieta (17 refeições registradas), 2 registrando peso (7 pesagens); checkpoints da rota, fotos e cardápio semanal com 0 linhas.

Pendentes de planos anteriores (aprovados e não executados): nenhum

Como o uso real está concentrado no **treino** (sessão → finalizar → resumo), a maior parte dos itens mira esse caminho.

## Itens

### 1. [direto] Criar `docs/MELHORIAS.md`

- **Dor:** sem histórico, a rotina não sabe o que já foi feito, rejeitado ou adiado e tende a repetir sugestões.
- **Mudança:** criar `docs/MELHORIAS.md` com as seções Feitas / Rejeitadas / Adiadas (vazias), no formato da skill.
- **Onde:** `docs/MELHORIAS.md`
- **Rota:** direto — só documentação, não toca no app.
- **Aprovação:** [x]

### 2. [direto] Botões −/+ da série atual com 44 px

- **Dor:** os −/+ de kg e reps são os controles mais tocados do app, usados com a mão suada entre séries, e têm 36 px — abaixo dos 44 px que o próprio app define como mínimo (`tap-target`).
- **Mudança:** coluna do grid de 36 px → 44 px e `StepButton` de `size-9` → `size-11`.
- **Onde:** `src/components/session/SetFields.tsx:189`, `:217`
- **Rota:** direto — só dimensão num componente.
- **Aprovação:** [x]

### 3. [direto] Séries pendentes legíveis e anunciadas pelo leitor de tela

- **Dor:** as próximas séries ("80 kg × 8") aparecem em `text-muted-foreground/60` (contraste 2,85:1), difíceis de ler na academia; e o `aria-label="Editar série 2"` apaga o peso, as reps e o selo de PR para quem usa VoiceOver/TalkBack.
- **Mudança:** pendentes em `text-muted-foreground` (6:1), mantendo o círculo tracejado como sinal de "pendente"; trocar o `aria-label` por um prefixo visualmente oculto para que os valores sejam lidos.
- **Onde:** `src/components/session/SetRows.tsx:33`, `:95-96`
- **Rota:** direto — cor e texto acessível de um componente, sem lógica.
- **Aprovação:** [x]

### 4. [direto] Perfil: mostrar erro com "Tentar de novo" em vez de skeleton eterno

- **Dor:** sem sinal ou com erro do Supabase, a tela de Perfil pulsa para sempre, sem mensagem nem saída.
- **Mudança:** antes do skeleton, se `profileQuery.isError`, renderizar o `QueryError` já existente com `refetch`.
- **Onde:** `src/routes/_authenticated/perfil.tsx:164`, `:187`
- **Rota:** direto — mensagem de erro numa tela, com componente existente.
- **Aprovação:** [x]

### 5. [preview] "Tentar de novo" que realmente tenta de novo (treinos e rotinas)

- **Dor:** depois de uma única falha de rede, o cache guarda a promessa rejeitada: Início, Treino e histórico continuam com erro, o botão "Tentar de novo" não faz nada e "Iniciar treino" falha mesmo com o sinal de volta, até fechar o app.
- **Mudança:** em `getWorkoutLog` e `all()`, limpar `inflight` quando a busca falha (`.catch(e => { inflight = null; throw e; })`).
- **Onde:** `src/lib/data/workouts.ts:12-22`, `src/lib/data/routines.ts:8-17`
- **Rota:** preview — camada de dados usada por várias telas; só se vê falhando a rede de verdade.
- **Aprovação:** [x]

### 6. [preview] Finalizar cedo não pode registrar séries que a pessoa não fez

- **Dor:** com prescrição, toda série já nasce com kg e reps preenchidos. Ao finalizar cedo, o diálogo diz "N séries preenchidas e não marcadas — incluir?", e o botão em destaque ("Incluir") grava séries que nunca aconteceram. A mesma contagem liga o aviso de "sair do site?" desde o primeiro segundo.
- **Mudança:** `filledUncheckedSets` passa a contar só séries cujo kg/reps a pessoa alterou em relação à sugestão (`sugPeso`/`sugReps`).
- **Onde:** `src/lib/session-state.ts:309-316` (e o uso em `src/routes/_authenticated/sessao.tsx` ~410 e ~1695)
- **Rota:** preview — regra de negócio que decide o que é gravado no histórico.
- **Aprovação:** [x]

### 7. [preview] Sinal fraco na academia não pode travar o fim do treino

- **Dor:** a fila offline só é usada quando `navigator.onLine === false`. Com sinal "fantasma" (conectado mas sem internet), `saveWorkout` falha e a pessoa fica presa na sessão com "Não foi possível salvar". Quando entra na fila, o resumo abre com 0 kg, 0 séries e "—", sem explicação.
- **Mudança:** se `saveWorkout` falhar, enfileirar (`enqueueWorkout`) e seguir para o resumo; o resumo lê o treino da fila quando ele ainda não está no servidor e mostra "Salvo no aparelho, sincroniza quando voltar o sinal". Também bloquear duplo toque no Finalizar enquanto salva.
- **Onde:** `src/routes/_authenticated/sessao.tsx:1098-1217`, `src/routes/_authenticated/resumo.$id.tsx:72-128`, `src/lib/offline-queue.ts`
- **Rota:** preview — duas telas e o fluxo de gravação; só se testa com rede ruim.
- **Aprovação:** [x]

### 8. [preview] Tela Treino com sessão em andamento: um só caminho para retomar

- **Dor:** com treino em andamento, o botão da rotina diz "Retomar no player" mas está desabilitado; e o card do coach continua mostrando "Manter plano" / "Sessão mais leve", que levam de volta ao treino antigo em silêncio, descartando a escolha.
- **Mudança:** tirar `|| active` do `disabled` do botão "Retomar" e esconder o `TodayCoachCard` enquanto houver sessão ativa (o banner do topo já é a ação única).
- **Onde:** `src/routes/_authenticated/treino.tsx:390-407`, `:665-672`
- **Rota:** preview — muda navegação e o fluxo entre Treino e Sessão.
- **Aprovação:** [x]

### 9. [preview] Peso e treino manual registrados no dia certo (fuso local)

- **Dor:** depois das 21h no Brasil, o peso lançado pelo card vai para o dia seguinte (data em UTC) e a barra rápida mostra "Ainda não registrado" hoje; o treino manual sugere "amanhã" como data e permite escolhê-la. Na Holanda, entre 0h e 2h, acontece o inverso.
- **Mudança:** trocar `new Date().toISOString().slice(0, 10)` pelo helper de data local que o app já usa (`isoDay`).
- **Onde:** `src/components/BodyWeightCard.tsx:45`, `src/components/ManualWorkoutSheet.tsx:31`
- **Rota:** preview — lógica de data que grava no banco, em dois componentes.
- **Aprovação:** [x]

### 10. [preview] Aceitar casas decimais nos campos de peso e quantidade

- **Dor:** não dá para digitar 72,5 kg no treino manual e na edição de sessão, nem "0,5" na quantidade de um ingrediente: a vírgula ou o ponto somem enquanto se digita (o campo converte para número a cada tecla); o treino manual aceita peso negativo.
- **Mudança:** guardar o texto digitado como rascunho, aceitar vírgula ou ponto, converter e limitar a ≥ 0 no blur/salvar.
- **Onde:** `src/components/ManualWorkoutSheet.tsx:202-207`, `src/routes/_authenticated/progresso.$id.tsx:116-131`, `:310`, `src/components/AddMealSheet.tsx:300-306`
- **Rota:** preview — formulários e validação em três lugares.
- **Aprovação:** [x]

## Fora desta rodada (candidatos para as próximas)

- Rota: erro de carregamento cai no estado vazio "Sua rota começa aqui" e convida a mapear uma rota duplicada (`rota.index.tsx:68-70`).
- Excluir refeição e itens da lista de compras sem desfazer (`MealEntryRow.tsx`, `dieta.market.tsx`).
- Editor de rotina aceita 0 séries e reps mín > máx (`rotina.$id.tsx:557-577`).
- Trocar exercício descarta as séries já feitas, sem desfazer (`sessao.tsx:785-805`).
- Erros da IA do plano aparecem em inglês para pt/nl (`plano.tsx:162`, `:182`).
- Fonte Archivo vinda do Google bloqueia a renderização e não vai para o cache offline (`__root.tsx:137`, `vite.config.ts`).
- Contraste do roxo em texto pequeno abaixo de 4,5:1 (`--primary` em `src/styles.css`).
- "Nota da RPE em um toque" e a linha "próxima série" após a RPE, descritas em `docs/plano-sessao-treino.md`, não estão no código.

## Como aprovar

Marque `[x]` nos itens aprovados. Para trocar a rota, edite a etiqueta no título (`[direto]` ↔ `[preview]`). Itens não marcados são ignorados e podem voltar em planos futuros.
